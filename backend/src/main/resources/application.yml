spring:
  application:
    name: devkit-backend

  datasource:
    url: jdbc:postgresql://192.168.1.110:5444/usedevkit?ssl=false&serverTimezone=America/Sao_Paulo
    username: admin
    password: Rapidex@727204
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        query.fail_on_pagination_over_collection_fetch: false
        query.in_clause_parameter_padding: true
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

# Management endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

# API Documentation
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

# Logging
logging:
  level:
    com.devkit: DEBUG
    org.springframework.data.jpa.repository: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# DevKit Configuration
devkit:
  security:
    jwt:
      secret: ${JWT_SECRET:this-is-a-dev-secret-key-of-exactly-64-bytes-for-hs512-algo-1234567890abcdef}
      access-token-expiration: 900000    # 15 minutes
      refresh-token-expiration: 604800000 # 7 days
    api-key:
      expiration-days: 365
    encryption:
      master-key: ${ENCRYPTION_MASTER_KEY:this-should-be-32-bytes-long-for-aes-256}
      algorithm: AES/GCM/NoPadding
  cache:
    feature-flag-ttl: 60   # seconds
    config-ttl: 300        # seconds (5 minutes)
  audit:
    enabled: true
    retention-days: 90
  secrets:
    aws:
      enabled: false
      region: us-east-1
      endpoint: ""
  notifications:
    secret-rotation:
      enabled: false
      channel: email
      recipient: ""
